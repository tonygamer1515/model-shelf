# local scratch, never commit
.DS_Store
*.pyc
__pycache__/
node_modules/

# Generated by scripts/build_site.py. CI rebuilds these on every deploy, so a
# committed copy would shadow the fresh build and race the Pages deployment.
index.html
data/models.json
